// window.addEventListener('unload', function() {
//   if (sessionStorage){
//         var name = sessionStorage.getItem("name")
//         var refresh = sessionStorage.getItem("refresh")
//         timerElt.textContent = "Un vélo réservé à la station : " + name + " pour " + min + " MIN " + sec + " S";
//       } else {
//         timerElt.textContent = "Vous n'avez pas de reservation"
//       }
//     });


    // //sessionStorage
    // var min = sessionStorage.setItem("min", min);
    // var sec = sessionStorage.setItem("sec", sec);
    // min = sessionStorage.getItem("min");
    // sec = sessionStorage.getItem("sec");
